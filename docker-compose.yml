services:
  medimate-client:
    
    image: ghcr.io/tritruong666/medimate-client:latest
    container_name: medimate-client
    restart: always
    networks:
      - medimate-network
      - web_network

networks:
  medimate-network:
    external: true
  web_network: 
    external: true
    